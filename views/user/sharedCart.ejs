
<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Shared Cart</title>
  <style>
    /* Reset */
    * {
      box-sizing: border-box;
      margin: 0;
      padding: 0;
      font-family: 'Lexend Deca', sans-serif, 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    }

    body {
      background: #f9f9f9;
      color: #333;
      padding: 40px 20px;
      display: flex;
      justify-content: center;
      align-items: flex-start;
      min-height: 100vh;
    }

    .container {
      background: #fff;
      border-radius: 8px;
      box-shadow: 0 6px 15px rgba(0,0,0,0.1);
      max-width: 720px;
      width: 100%;
      padding: 30px 40px;
    }

    h1 {
      font-size: 2.8rem;
      font-weight: 700;
      color: #4b953b;
      margin-bottom: 30px;
      text-align: center;
    }

    p.message {
      font-size: 1.3rem;
      text-align: center;
      color: #777;
      padding: 40px 0;
    }

    ul.cart-list {
      list-style: none;
      display: flex;
      flex-direction: column;
      gap: 20px;
      margin-bottom: 35px;
    }

    li.cart-item {
      display: flex;
      align-items: center;
      gap: 25px;
      background: #f4f9f4;
      border-radius: 10px;
      padding: 15px 20px;
      box-shadow: 0 1px 6px rgba(75, 149, 59, 0.15);
      transition: box-shadow 0.3s ease;
    }

    li.cart-item:hover {
      box-shadow: 0 4px 15px rgba(75, 149, 59, 0.3);
    }

    .product-image {
      width: 90px;
      height: 70px;
      object-fit: contain;
      border-radius: 6px;
      background: #fff;
      border: 1.5px solid #a9d7a9;
      flex-shrink: 0;
    }

    .item-details {
      flex: 1;
    }

    .product-name {
      font-size: 1.4rem;
      font-weight: 600;
      color: #2e6f2e;
      margin-bottom: 6px;
    }

    .quantity-price {
      font-size: 1.1rem;
      color: #4b754b;
    }

    h3.total {
      font-size: 2.2rem;
      text-align: right;
      font-weight: 700;
      color: #4b953b;
      border-top: 3px solid #4b953b;
      padding-top: 18px;
      user-select: none;
    }

    @media (max-width: 480px) {
      .container {
        padding: 20px;
      }

      h1 {
        font-size: 2rem;
        margin-bottom: 20px;
      }

      li.cart-item {
        flex-direction: column;
        align-items: flex-start;
        gap: 12px;
      }

      .product-image {
        width: 100%;
        height: auto;
        border-radius: 8px;
      }

      .quantity-price {
        font-size: 1rem;
      }

      h3.total {
        font-size: 1.8rem;
        text-align: center;
        border-top: none;
        padding-top: 10px;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Shared Cart</h1>

    <% if (!cartItems || cartItems.length === 0) { %>
      <p class="message">No items found in the shared cart.</p>
    <% } else { %>
      <ul class="cart-list">
        <% cartItems.forEach(item => { %>
          <li class="cart-item">
            <% if (item.product.productImage && item.product.productImage.length > 0) { %>
              <img src="<%= item.product.productImage[0] %>" alt="<%= item.product.productName %>" class="product-image" />
            <% } else { %>
              <div class="product-image" style="display:flex;justify-content:center;align-items:center;color:#bbb;font-size:0.9rem;">
                No Image
              </div>
            <% } %>
            <div class="item-details">
              <div class="product-name"><%= item.product.productName %></div>
              <div class="quantity-price">
                Quantity: <%= item.quantity %> &nbsp;|&nbsp; Price: ₹<%= item.totalPrice.toFixed(2) %>
              </div>
            </div>
          </li>
        <% }) %>
      </ul>

      <h3 class="total">Total: ₹<%= grandTotal.toFixed(2) %></h3>
    <% } %>
  </div>
</body>
</html>
